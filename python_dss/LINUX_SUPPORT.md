# Кроссплатформенность StabLimit

## ✅ Текущий статус

**Приложение StabLimit является кроссплатформенным!**

- ✅ **UI и интерфейс** - работают на Windows, Linux и macOS
- ✅ **Работа с Excel** - работает на всех платформах
- ✅ **Визуализация графиков** - работает на всех платформах
- ✅ **Вся бизнес-логика** - работает на всех платформах
- ⚠️ **RASTR расчеты** - доступны только на Windows

## 🖥️ Поддерживаемые платформы

### Windows
- ✅ Полная функциональность
- ✅ Все расчеты с RASTR
- ✅ Все модули работают

### Linux / macOS
- ✅ UI и интерфейс
- ✅ Просмотр и редактирование данных
- ✅ Работа с Excel файлами
- ✅ Визуализация результатов
- ⚠️ Расчеты требуют Windows с RASTR

## 🔧 Технические детали

### Кроссплатформенные компоненты

1. **UI Framework**
   - `tkinter` - встроен в Python, работает везде
   - `customtkinter` - кроссплатформенный
   - `Pillow` - кроссплатформенная работа с изображениями

2. **Работа с данными**
   - `openpyxl` - кроссплатформенная работа с Excel
   - `pathlib` - кроссплатформенная работа с путями
   - Стандартные библиотеки Python

3. **Визуализация**
   - `matplotlib` - кроссплатформенная
   - `plotly` - кроссплатформенная
   - `numpy` - кроссплатформенная

### Windows-специфичные компоненты

1. **RASTR интеграция**
   - `pywin32` - только для Windows
   - COM-интерфейс - только для Windows
   - RASTR (ПК RUSTab) - только для Windows

## 🚀 Установка и запуск

### Windows (полная функциональность)

```bash
cd python_dss
pip install -r requirements.txt
python main.py
```

### Linux / macOS (UI и работа с данными)

```bash
cd python_dss
# Установка без pywin32 (он установится только на Windows автоматически)
pip install -r requirements.txt
python main.py
```

**Примечание:** При попытке выполнить расчеты на Linux/macOS вы получите понятное сообщение о том, что RASTR недоступен на данной платформе.

## 📋 Поведение на разных платформах

### Windows
- Все функции доступны
- Расчеты выполняются через RASTR
- Полная функциональность

### Linux / macOS
- UI запускается и работает
- Можно загружать и просматривать файлы
- Можно работать с Excel
- При попытке загрузить файлы, требующие RASTR (сечения, ремонтные схемы, графический вывод, ПА), появится сообщение:
  > "RASTR недоступен на данной платформе. Для работы с файлами [тип файла] требуется Windows с установленным RASTR."
- При попытке запустить расчеты появится сообщение:
  > "RASTR недоступен на данной платформе. Для выполнения расчетов требуется Windows с установленным RASTR (ПК RUSTab)."

## 🔍 Как это работает

### Условный импорт

Приложение использует условный импорт для `RastrOperations`:

```python
try:
    from rastr_operations import RastrOperations, RASTR_AVAILABLE
except ImportError:
    RastrOperations = None
    RASTR_AVAILABLE = False
```

### Проверка доступности

Перед использованием RASTR проверяется его доступность:

```python
if not RASTR_AVAILABLE or RastrOperations is None:
    raise RastrUnavailableException(
        "RASTR недоступен на данной платформе. "
        "Для работы требуется Windows с установленным RASTR."
    )
```

### Обработка ошибок

Все ошибки `RastrUnavailableException` обрабатываются централизованно и показывают пользователю понятные сообщения.

## 💡 Варианты использования на Linux/macOS

### 1. Разработка и тестирование UI
- Тестирование интерфейса
- Проверка валидации данных
- Тестирование работы с Excel

### 2. Просмотр и подготовка данных
- Просмотр загруженных файлов
- Подготовка данных для расчетов
- Экспорт/импорт данных

### 3. Анализ результатов
- Просмотр сохраненных результатов
- Визуализация графиков
- Работа с Excel отчетами

## 🎯 Рекомендации

1. **Для разработки:** Используйте Linux/macOS для разработки UI и логики
2. **Для расчетов:** Используйте Windows с установленным RASTR
3. **Для тестирования:** Тестируйте UI на всех платформах, расчеты - только на Windows

## 📚 Дополнительная информация

- `requirements.txt` - зависимости с условной установкой `pywin32` только на Windows
- `rastr_operations/rastr_operations.py` - проверка доступности RASTR
- `utils/exceptions.py` - исключение `RastrUnavailableException`
- `utils/error_handler.py` - обработка ошибок недоступности RASTR
