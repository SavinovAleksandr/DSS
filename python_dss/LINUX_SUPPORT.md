# Поддержка Linux

## ⚠️ Текущая ситуация

**Программа НЕ будет работать на Linux в полном объеме** по следующим причинам:

### Проблемы:

1. **RASTR - Windows-специфичное ПО**
   - RASTR (ПК RUSTab) доступен только для Windows
   - Использует COM-интерфейс, который доступен только на Windows
   - Все расчеты критически зависят от RASTR

2. **pywin32 - только для Windows**
   - Библиотека `pywin32` работает только на Windows
   - Используется для доступа к COM-интерфейсу RASTR

3. **COM-интерфейс**
   - COM (Component Object Model) - технология Microsoft, доступна только на Windows

## ✅ Что работает на Linux:

- **UI (tkinter)** - работает на Linux
- **Работа с Excel (openpyxl)** - работает на Linux
- **Визуализация (matplotlib)** - работает на Linux
- **Вся бизнес-логика** - работает на Linux

## 🔧 Варианты решения для Linux:

### Вариант 1: Мок-версия для тестирования

Создать версию с мок-объектами `RastrOperations`, которая:
- Позволяет тестировать UI и логику
- Читает файлы напрямую (без COM)
- Возвращает тестовые данные

### Вариант 2: Прямая работа с файлами RASTR

Создать альтернативную реализацию, которая:
- Читает файлы `.rst`, `.rg2`, `.scn`, `.vrn` напрямую
- Парсит формат файлов RASTR
- Выполняет расчеты без COM-интерфейса

**Проблема:** Требует глубокого знания формата файлов RASTR и алгоритмов расчетов.

### Вариант 3: Использование Wine

Запуск Windows-версии RASTR через Wine:
- Установить Wine на Linux
- Установить RASTR через Wine
- Адаптировать COM-вызовы для работы через Wine

**Проблема:** Нестабильно, может не работать.

### Вариант 4: Виртуальная машина

Запуск программы в Windows VM на Linux:
- Установить Windows в виртуальной машине
- Установить RASTR в VM
- Запускать программу в VM

## 📋 Рекомендации:

1. **Для разработки/тестирования:** Использовать мок-версию (Вариант 1)
2. **Для продакшена:** Использовать Windows или Windows VM
3. **Для будущего:** Рассмотреть создание альтернативной реализации с прямым чтением файлов

## 🚀 Быстрый старт на Linux (мок-версия):

```bash
# Установка зависимостей (без pywin32)
pip install openpyxl matplotlib numpy

# Программа запустится, но расчеты будут использовать мок-объекты
python main.py
```

**Примечание:** Мок-версия позволит тестировать UI и логику, но не выполнит реальные расчеты.

