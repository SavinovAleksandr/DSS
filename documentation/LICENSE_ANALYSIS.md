# Анализ механизма проверки лицензии DynStabSpace

## 📋 Обзор

Механизм проверки лицензии реализован в методе `License()` класса `MainWindow` и вызывается при запуске приложения.

---

## 🔍 Код проверки лицензии

### Расположение:
- **Файл:** `source_code/Views/MainWindow.cs`
- **Метод:** `private bool License()` (строки 53-79)
- **Вызов:** В конструкторе `MainWindow()` (строка 29)

### Полный код метода:

```csharp
private bool License()
{
    bool flag = false;
    string machineName = Environment.MachineName;
    
    // Проверка существования файла лицензии
    if (File.Exists("C:\\ПАРУС 6\\licence.txt"))
    {
        // Разделение имени машины на части
        int length = machineName.Length;
        int num = Convert.ToInt32(length / 3);        // Первая треть
        int num2 = length - 2 * num;                  // Остаток
        
        string text = machineName.Substring(0, num);  // Первая часть
        string text2 = machineName.Substring(num, num); // Вторая часть
        
        // Обработка дефисов во второй части
        if (text2.Substring(0, 1) == "-")
        {
            text += "-";
            text2 = text2.Replace("-", "");
        }
        
        string text3 = machineName.Substring(length - num2, num2); // Третья часть
        
        // Обработка дефисов в третьей части
        if (text3.Substring(0, 1) == "-")
        {
            text2 += "-";
            text3 = text3.Replace("-", "");
        }
        
        // Чтение файла лицензии
        string[] array = File.ReadAllLines("C:\\ПАРУС 6\\licence.txt").ToArray();
        
        // Проверка соответствия частей имени машины в файле
        return array[43].Contains(text) && 
               array[45].Contains(text2) && 
               array[46].Contains(text3) && 
               array[52].Contains("p5");
    }
    
    throw new UserLicenseException("Файл лицензии не обнаружен");
}
```

---

## 🔐 Алгоритм проверки

### Шаг 1: Получение имени компьютера
```csharp
string machineName = Environment.MachineName;
```
- Используется имя компьютера Windows
- Пример: "DESKTOP-ABC123" или "COMPUTER-NAME"

### Шаг 2: Разделение имени на части

Имя машины делится на 3 части:
- **text** (первая часть): первые `length/3` символов
- **text2** (вторая часть): следующие `length/3` символов  
- **text3** (третья часть): оставшиеся символы

**Пример:**
- Имя: "DESKTOP-ABC123" (15 символов)
- `num = 15/3 = 5`
- `num2 = 15 - 2*5 = 5`
- text = "DESKT" (первые 5)
- text2 = "OP-AB" (следующие 5)
- text3 = "C123" (оставшиеся 5)

### Шаг 3: Обработка дефисов

Если вторая или третья часть начинается с дефиса "-", он переносится:
- Дефис из text2 → добавляется к text
- Дефис из text3 → добавляется к text2

### Шаг 4: Чтение файла лицензии

```csharp
string[] array = File.ReadAllLines("C:\\ПАРУС 6\\licence.txt").ToArray();
```

**Важно:** 
- Файл должен находиться по пути: `C:\ПАРУС 6\licence.txt`
- Файл читается как текстовый (построчно)

### Шаг 5: Проверка соответствия

Проверяются **4 условия**:

1. **Строка 43** (`array[43]`) должна содержать `text` (первую часть имени)
2. **Строка 45** (`array[45]`) должна содержать `text2` (вторую часть имени)
3. **Строка 46** (`array[46]`) должна содержать `text3` (третью часть имени)
4. **Строка 52** (`array[52]`) должна содержать строку `"p5"`

**Все 4 условия должны быть выполнены** (логическое И).

---

## ⚠️ Особенности реализации

### 1. Жестко заданный путь
- Путь к файлу лицензии жестко прописан: `"C:\\ПАРУС 6\\licence.txt"`
- Не работает на других дисках или в других папках

### 2. Зависимость от имени компьютера
- Лицензия привязана к конкретному имени компьютера
- При смене имени компьютера лицензия перестанет работать

### 3. Простая проверка
- Используется только `Contains()` - не криптографическая проверка
- Нет проверки подписи или хеша
- Нет защиты от модификации файла

### 4. Текстовый формат
- Файл читается как текстовый (`ReadAllLines`)
- Ожидается, что это текстовый файл с минимум 53 строками

### 5. Магические числа
- Строки 43, 45, 46, 52 - жестко заданы в коде
- Нет объяснения, почему именно эти строки

---

## 🚨 Проблемы безопасности

### Слабые места:

1. **Нет криптографии**
   - Нет подписи файла
   - Нет шифрования
   - Легко подделать

2. **Простая проверка**
   - Используется только `Contains()`
   - Можно добавить нужные строки в файл

3. **Жестко заданный путь**
   - Легко найти файл лицензии
   - Нет защиты от копирования

4. **Нет проверки целостности**
   - Можно модифицировать файл
   - Нет проверки на изменение

5. **Зависимость от имени машины**
   - Легко узнать алгоритм
   - Можно предсказать, что нужно в файле

---

## 📊 Структура файла лицензии

Судя по коду, файл `licence.txt` должен иметь структуру:

```
[строка 0]
[строка 1]
...
[строка 43]  ← должна содержать первую часть имени машины
[строка 44]
[строка 45]  ← должна содержать вторую часть имени машины
[строка 46]  ← должна содержать третью часть имени машины
[строка 47]
...
[строка 52]  ← должна содержать "p5"
...
```

**Минимум 53 строки** (индексация с 0).

---

## 🔧 Интеграция в приложение

### Вызов проверки:

```csharp
public MainWindow()
{
    InitializeComponent();
    try
    {
        if (License())  // ← Проверка лицензии
        {
            ((FrameworkElement)this).DataContext = new DataInfo();
            return;
        }
        throw new UserLicenseException("Некорректный файл лицензии");
    }
    catch (Exception ex)
    {
        MessageBox.Show("Ошибка при выполнении операции!" + 
                       Environment.NewLine + Environment.NewLine + 
                       ex.Message, "Error", 
                       (MessageBoxButton)0, (MessageBoxImage)16);
        ((Window)this).Close();  // ← Закрытие приложения при ошибке
    }
}
```

### Обработка ошибок:

- Если лицензия не найдена → `UserLicenseException("Файл лицензии не обнаружен")`
- Если лицензия неверна → `UserLicenseException("Некорректный файл лицензии")`
- При любой ошибке → показ сообщения и закрытие приложения

---

## 📝 Выводы

### Особенности механизма:

1. ✅ **Простота** - легко понять и реализовать
2. ✅ **Привязка к машине** - лицензия работает только на одном компьютере
3. ❌ **Слабая защита** - нет криптографии
4. ❌ **Легко обойти** - можно подделать файл
5. ❌ **Жестко заданный путь** - негибко

### Рекомендации для улучшения:

1. Использовать криптографическую подпись
2. Хешировать имя машины
3. Проверять целостность файла
4. Использовать более сложный алгоритм
5. Добавить проверку срока действия
6. Использовать онлайн-проверку (опционально)

---

## 🔍 Интересные факты

1. **Файл licence.txt в проекте** - это JPEG изображение, а не текстовый файл
   - Возможно, это пример или тестовый файл
   - Реальный файл должен быть текстовым

2. **Строка "p5"** - возможно, версия лицензии или тип лицензии
   - Неясно из кода, что означает "p5"

3. **Путь "ПАРУС 6"** - возможно, связан с другой программой
   - "ПАРУС" может быть названием другой системы
   - Программа может быть частью комплекса "ПАРУС"

---

**Примечание:** Этот анализ предназначен только для образовательных целей и понимания работы механизма лицензирования. Не используйте эту информацию для обхода защиты программного обеспечения.

